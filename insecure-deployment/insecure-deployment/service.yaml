apiVersion: v1
kind: Service
metadata:
  name: recipe-service
spec:
  type: NodePort
  selector:
    app: recipe
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
